let initialState = {
    friends: [
        { id: 1, name: 'Alex' },
        { id: 2, name: 'Ganry' },
        { id: 3, name: 'John' },
        { id: 4, name: 'Richmond' },
    ]
}

let friendsReducer = (state = initialState, action) => {
    return state
}

export default friendsReducer;